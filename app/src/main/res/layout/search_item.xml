<?xml version="1.0" encoding="utf-8"?>

<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="food"
            type="com.example.myhealthapp.network.model.Food"/>

    </data>
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center">

        <TextView
            android:text="@string/listitem"
            android:layout_width="0sp"
            android:layout_height="wrap_content"
            android:id="@+id/search_item_name"
            android:textAppearance="@style/TextAppearance.AppCompat.Body2"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="16dp"
            android:layout_marginTop="8dp"
            app:layout_constraintTop_toTopOf="parent"
            android:textSize="18sp"
            android:textColor="@color/white"
            android:layout_marginBottom="8dp"
            android:layout_weight="1"
            android:layout_marginEnd="15sp"
            app:layout_constraintBottom_toBottomOf="parent"/>
        <TextView
            android:textSize="14sp"
            android:text="@string/kcal"
            android:textColor="@color/white"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" android:id="@+id/search_item_kcal"
            app:layout_constraintEnd_toEndOf="parent" android:layout_marginEnd="16dp"
            app:layout_constraintBottom_toBottomOf="@+id/search_item_name"
            app:layout_constraintTop_toTopOf="@+id/search_item_name"
            android:textAppearance="@style/TextAppearance.AppCompat.Body1"/>
    </LinearLayout>
</layout>